{
  "name": "peer-drop",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:8080",
  "dependencies": {
    "file-saver": "^2.0.1",
    "file-to-array-buffer": "^1.0.0",
    "node-sass": "^4.12.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.0",
    "simple-peer": "^9.3.0",
    "socket.io-client": "^2.2.0",
    "express": "^4.17.0",
    "socket.io": "^2.2.0",
    "useragent": "^2.3.0"
  },
  "devDependencies": {
    "nodemon": "^1.19.0"
  },
  "scripts": {
    "start": "nodemon server.js && react-scripts start",
    "build": "react-scripts build && PORT=443 node server.js"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
